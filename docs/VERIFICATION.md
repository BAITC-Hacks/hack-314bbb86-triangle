# Проверка — 23 сентября 2026

Среда: Windows, Node.js v22.12.0. Команда: `npm.cmd test`.

**21 тест прошёл, 0 ошибок.**

Контрольные значения:

| Сценарий | Бюджет | Score | Критические |
|---|---:|---:|---:|
| Исходные данные (база, не завершённый сценарий) | 0 | 52.55768 | 2 |
| Пример организаторов | 95 | 56.54307 | 0 |
| Пример с заменой M5 на M3 в Нуре | 100 | 57.20556 | 0 |

Браузерная проверка в Codex in-app browser:

- Начальная карта и значения совпадают с моделью.
- Кнопка анализа неактивна до пяти решений.
- Пример → серверный отчёт: 56,54, бюджет 95, 0 критических, 1 синергия.
- Сохранён сценарий «Приоритет — Нура»; строка сравнения показывает те же числа.
- Применена рекомендация → новый анализ: 57,21, бюджет 100.
- После сброса и выбора автобусных полос остаток 82, ЛРТ заблокирован,
  показана причина несовместимости; анализ при одном решении заблокирован.
- Проверены 1440×1000 и 390×844: карта/панели адаптируются;
  на мобильном document scrollWidth не превышает viewport.
- Консоль браузера: ошибок и предупреждений не обнаружено при основном пути.

После предоставления ключа выполнен настоящий запрос через `/api/analyze`:
HTTP 200, `mode: ai`, `model: gpt-4.1-mini`, Score 56.54307.
Модель вернула структурированное объяснение улучшения Нуры, общегородского
результата и оставшегося бюджета. Ключ сохранён только в локальном `.env`.
Формат запросов/ответов, недоступность, некорректный ответ, incomplete и fallback
дополнительно покрыты тестами с подменой провайдера.
Печатная версия и JSON-экспорт реализованы;
сохранение PDF через системный диалог вручную не проверялось.
