{
  "name": "akim-na-5-chasov",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Triangle — AI-симулятор городских решений",
  "engines": { "node": ">=22" },
  "scripts": {
    "start": "node --env-file-if-exists=.env server.mjs",
    "dev": "node --env-file-if-exists=.env --watch server.mjs",
    "test": "node --test tests/*.test.mjs"
  }
}
